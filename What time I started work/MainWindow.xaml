<Controls:MetroWindow x:Class="What_time_I_started_work.MainWindow"        
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="MainWindow"
                      Width="500"
                      Height="100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" HorizontalAlignment="Left" Margin="10" Grid.Row="0" Text="{Binding Path=Label}" TextWrapping="Wrap" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" HorizontalAlignment="Left" Margin="10" TextWrapping="Wrap"  Text="{Binding Path=Content1, Mode=OneWay}" VerticalAlignment="Center" Width="120" IsReadOnly="True"/>

    </Grid>
</Controls:MetroWindow>
